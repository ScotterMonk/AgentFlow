# User Query - GUI Favorites Feature

Turn `.roo/docs/plans/plan-add-favorites.md` (detailed specification) into an executable plan for implementation.

The specification describes a comprehensive favorites feature for the GUI including:
- Config parsing for `folders_faves` key
- GUI state management for favorite folders
- UI elements (buttons and per-folder star toggle)
- Persistence to config.txt
- Load/save functionality
- pytest testing